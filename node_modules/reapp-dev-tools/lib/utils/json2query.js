'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol ? "symbol" : typeof obj; };

exports.json2query = json2query;
function json2query(source) {
    var query = [];
    Object.keys(source).filter(validValues(source)).forEach(function (key) {
        var rule = ['$' + key, queryValue(source[key])];
        query.push(rule.join(':'));
    });

    query = query.join(';');

    if (query.length) {
        query += ';';
    }
    // console.log(query);
    return query;
}

function queryValue(value) {
    // encodeURIComponent
    if (value.toString().indexOf('/') !== -1) {
        return '"' + value + '"';
    }
    return value;
}

function validValues(source) {
    return function (key) {
        return ['function', 'object'].indexOf(_typeof(source[key])) === -1;
    };
}